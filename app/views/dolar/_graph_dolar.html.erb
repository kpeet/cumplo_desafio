<div >
  <%= line_chart @tweets_count,
                title: 'Gráfico valor dolar en pesos(cl)',
                colors: ['green'],
                library: {
                    tooltip: {
                        pointFormat: 'Times Bought'
                    }
                } %>
</div>